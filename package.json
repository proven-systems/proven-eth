{
  "name": "proven",
  "version": "0.1.0",
  "private": true,
  "description": "Proven Suite",
  "devDependencies": {
    "mkdirp": "^0.5.1",
    "sol-merger": "^0.1.2",
    "solc": "^0.4.9"
  },
  "license": "AGPL-3.0",
  "scripts": {
    "build": "node build.js",
    "build-contracts": "mkdir -p ./build/merged ; sol-merger './contracts/*.sol' ./build/merged",
    "deploy": "node deploy.js",
    "clean": "rm -rf output"
  },
  "dependencies": {
    "solium": "^1.0.9",
    "truffle": "^4.0.1",
    "web3": "^0.18.0-beta",
    "zeppelin-solidity": "^1.4.0"
  }
}
