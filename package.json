{
  "name": "proven",
  "version": "0.1.0",
  "private": true,
  "description": "Proven Suite",
  "devDependencies": {
    "truffle": "^4.0.1",
    "zeppelin-solidity": "^1.4.0",
    "npm-watch": "^0.2.0"
  },
  "license": "AGPL-3.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "node deploy.js",
    "clean": "rm -rf output",
    "test": "truffle test",
    "watch": "npm-watch"
  },
  "watch": {
    "test": {
      "patterns": [
        "truffle",
        "test",
        "contracts"
      ],
      "extensions": "js,json,sol"
    }
  }
}
